diff --git a/tools/actions/session_manager.py b/tools/actions/session_manager.py
index 4e8426c..3e47fa7 100644
--- a/tools/actions/session_manager.py
+++ b/tools/actions/session_manager.py
@@ -66,6 +66,22 @@ def start(args, unlocked_cb=None, background=True):
         logging.error(f"Wayland socket '{wayland_socket_path}' doesn't exist; are you running a Wayland compositor?")
         sys.exit(1)
 
+    # NVIDIA Venus mode (nvidia_venus_socket set by waydroid-nvidia-setup):
+    # the guest renders through this socket; without it SurfaceFlinger can
+    # only crash-loop. Refuse to start and say exactly what is wrong.
+    venus_socket = tools.config.load(args)["waydroid"].get("nvidia_venus_socket")
+    if venus_socket:
+        import socket as _socket
+        sock = _socket.socket(_socket.AF_UNIX, _socket.SOCK_STREAM)
+        try:
+            sock.connect(venus_socket)
+        except OSError as e:
+            logging.error(f"Venus render server socket '{venus_socket}' is not accepting connections ({e.strerror}).")
+            logging.error("Start it as your desktop user: systemctl --user enable --now wd-venus.service")
+            sys.exit(1)
+        finally:
+            sock.close()
+
     waydroid_data = session["waydroid_data"]
     if not os.path.isdir(waydroid_data):
         os.makedirs(waydroid_data)
diff --git a/tools/helpers/gpu.py b/tools/helpers/gpu.py
index 92190ab..c1d96fe 100644
--- a/tools/helpers/gpu.py
+++ b/tools/helpers/gpu.py
@@ -23,10 +23,10 @@ def getDriNode(args):
     if node:
         if not os.path.exists(node):
             raise OSError("The specified drm_device {} does not exist".format(node))
+        # An explicitly configured drm_device is trusted as-is; the
+        # unsupported blacklist only guards auto-detection below.
         renderDev = os.path.basename(node)
-        if getKernelDriver(args, renderDev) not in unsupported:
-            return node, getCardFromRender(args, renderDev)
-        return "", ""
+        return node, getCardFromRender(args, renderDev)
 
     for node in sorted(glob.glob("/dev/dri/renderD*")):
         renderDev = os.path.basename(node)
diff --git a/tools/helpers/lxc.py b/tools/helpers/lxc.py
index d3f6c24..2983b0c 100644
--- a/tools/helpers/lxc.py
+++ b/tools/helpers/lxc.py
@@ -124,6 +124,25 @@ def generate_nodes_lxc_config(args):
     # NFC config
     make_entry("/system/etc/libnfc-nci.conf", options="bind,optional 0 0")
 
+    # NVIDIA Venus stack: host venus socket dir and our replacement guest
+    # libraries, bind-mounted over the image. Emitted only when
+    # nvidia_venus_socket is configured (written by waydroid-nvidia-setup),
+    # and deliberately NOT 'optional': a missing piece must fail container
+    # start loudly instead of surfacing as an opaque SurfaceFlinger crash
+    # loop deep inside the guest.
+    cfg = tools.config.load(args)
+    venus_socket = cfg["waydroid"].get("nvidia_venus_socket")
+    if venus_socket:
+        venus_dir = os.path.dirname(venus_socket)
+        nodes.append("lxc.mount.entry = {} dev/venus none rbind,create=dir 0 0".format(venus_dir))
+        nodes.append("lxc.mount.entry = /var/lib/waydroid/overlay/vendor/lib64/hw/vulkan.virtio.so vendor/lib64/hw/vulkan.virtio.so none rbind,create=file 0 0")
+        nodes.append("lxc.mount.entry = /var/lib/waydroid/nv/libgbm_mesa_wrapper.so vendor/lib64/libgbm_mesa_wrapper.so none rbind,create=file 0 0")
+        nodes.append("lxc.mount.entry = /var/lib/waydroid/nv/hwcomposer.waydroid.so vendor/lib64/hw/hwcomposer.waydroid.so none rbind,create=file 0 0")
+        nodes.append("lxc.mount.entry = /var/lib/waydroid/nv/libEGL_angle.so vendor/lib64/egl/libEGL_angle.so none rbind,create=file 0 0")
+        nodes.append("lxc.mount.entry = /var/lib/waydroid/nv/libGLESv1_CM_angle.so vendor/lib64/egl/libGLESv1_CM_angle.so none rbind,create=file 0 0")
+        nodes.append("lxc.mount.entry = /var/lib/waydroid/nv/libGLESv2_angle.so vendor/lib64/egl/libGLESv2_angle.so none rbind,create=file 0 0")
+        nodes.append("lxc.mount.entry = /var/lib/waydroid/nv/surfaceflinger system/bin/surfaceflinger none rbind,create=file 0 0")
+
     return nodes
 
 LXC_APPARMOR_PROFILE = "lxc-waydroid"
diff --git a/tools/services/hardware_manager.py b/tools/services/hardware_manager.py
index 072df41..7f74391 100644
--- a/tools/services/hardware_manager.py
+++ b/tools/services/hardware_manager.py
@@ -23,6 +23,8 @@ def start(args):
         cfg = tools.config.load(args)
         if cfg["waydroid"]["suspend_action"] == "stop":
             tools.actions.session_manager.stop(args)
+        elif cfg["waydroid"]["suspend_action"] == "none":
+            logging.debug("Ignoring suspend request (suspend_action = none)")
         else:
             tools.actions.container_manager.freeze(args)
 
